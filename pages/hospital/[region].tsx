import HeadInfo from "../../components/global/HeadInfo";

const RegionalHospital = () => {
  return (
    <>
      <HeadInfo
      // title={`${region} 지역의 상급종합병원`}
      // content={`선택하진 ${region} 지역의 상급종합병원입니다.`}
      />
      <div>Hospital</div>
    </>
  );
};

export default RegionalHospital;
